import React from 'react';
import PropTypes from 'prop-types';
import Widget from "../Widget";

const TopCommentsPanel = (props) => {
    const {} = props;
    return (
        <Widget>
            Top Comments
        </Widget>
    );
}

TopCommentsPanel.propTypes = {}

TopCommentsPanel.defaultProps = {}

export default TopCommentsPanel;