import React from 'react';
import Widget from "../Widget";

const VideoRatingWidget = (props) => {
    const {} = props;
    return (
        <Widget>
            Video Rating Widget
        </Widget>
    );
}

VideoRatingWidget.propTypes = {}

VideoRatingWidget.defaultProps = {}

export default VideoRatingWidget;